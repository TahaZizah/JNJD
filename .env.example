# ── PostgreSQL ─────────────────────────────────────────────
POSTGRES_DB=jnjd
POSTGRES_USER=jnjd_user
POSTGRES_PASSWORD=change_me_postgres

# ── Redis ──────────────────────────────────────────────────
REDIS_HOST=redis
REDIS_PORT=6379

# ── MinIO ──────────────────────────────────────────────────
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=change_me_minio
MINIO_ENDPOINT=http://minio:9000
MINIO_BUCKET=jnjd-proofs
MINIO_PUBLIC_ENDPOINT=http://localhost:9000   # override for prod with public domain

# ── Spring Backend ─────────────────────────────────────────
SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/jnjd
SPRING_DATASOURCE_USERNAME=jnjd_user
SPRING_DATASOURCE_PASSWORD=change_me_postgres

# ── JWT ────────────────────────────────────────────────────
JWT_SECRET=replace_with_64_char_random_hex_string_here_1234567890abcdef
JWT_EXPIRATION_MS=86400000   # 24 hours

# ── Admin credentials (seeded at startup) ─────────────────
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_me_admin_password

# ── Email (SMTP) — MailHog for local dev testing ───────────
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM=JNJD Registration <test@example.com>

# ── Frontend origin (CORS) ─────────────────────────────────
FRONTEND_ORIGIN=http://localhost:5173
